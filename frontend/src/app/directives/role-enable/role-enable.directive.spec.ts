import { RoleEnableDirective } from './role-enable.directive';

describe('RoleEnableDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleEnableDirective();
    expect(directive).toBeTruthy();
  });
});
