{
	"sortMiddleware": {
		"in": "query",
		"name": "sort",
		"type": "String",
		"description": "Results are sorted by params (field to be sorted, sort order **asc-ascendent** or **desc-descendent**)"
	},
	"filterMiddleware": {
		"in": "query",
		"name": "filter",
		"type": "String",
		"description": "Results filtered by params (it can be just a `fieldName=field` or a limit `birthdayDate=minDate,maxDate`)"
	}
}