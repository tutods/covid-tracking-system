{
	"notAuthenticated": {
		"description": "Session expires or not have login",
		"schema": {
			"type": "object",
			"properties": {
				"message": {
					"type": "string",
					"example": "Not authenticated"
				}
			}
		}
	},
	"unauthorized": {
		"description": "User don't have the necessary scopes",
		"schema": {
			"type": "object",
			"properties": {
				"message": {
					"type": "string",
					"example": "Not authorized"
				}
			}
		}
	}
}