securityDefinitions:
  JWT:
    type: apiKey
    name: session
    in: cookie
    description: |
      For accessiong the API with valid JWT Token you must be passed your token on header.
      To get your token see **/users/login** wiht your data
