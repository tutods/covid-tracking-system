import { EnableMenuDirective } from './enable-menu.directive';

describe('EnableMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableMenuDirective();
    expect(directive).toBeTruthy();
  });
});
