{
	"JWT": {
		"type": "apiKey",
		"name": "session",
		"in": "cookie",
		"description": [
			"For accessing the API with valid JWT Token you must be passed your token on header.",
			"To get your token see **/users/login** with your data"
		]
	}
}