{
	"successReset": {
		"description": "Email has been sent with further instructions",
		"schema": {
			"type": "object",
			"properties": {
				"status": {
					"type": "boolean",
					"default": true
				},
				"message": {
					"type": "string",
					"example": "Email has been sent with instructions"
				}
			}
		}
	},
	"invalidReset": {
		"description": "Invalid email or email not exists on database",
		"schema": {
			"type": "object",
			"properties": {
				"status": {
					"type": "boolean",
					"default": false
				},
				"message": {
					"type": "string",
					"example": "Invalid Credentials"
				}
			}
		}
	}
}