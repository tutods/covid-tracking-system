import { CanUseDirective } from './can-use.directive';

describe('CanUseDirective', () => {
  it('should create an instance', () => {
    const directive = new CanUseDirective();
    expect(directive).toBeTruthy();
  });
});
